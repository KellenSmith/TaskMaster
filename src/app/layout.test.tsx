import { describe, it } from "vitest";

describe("RootLayout", () => {
    it.todo("renders NavPanel and passes children through ServerContextWrapper");
    it.todo("exports metadata with title and description strings");
});
