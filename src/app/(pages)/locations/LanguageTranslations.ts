import { Language } from "@prisma/client";

const LanguageTranslations = {
    addLocation: {
        [Language.english]: "Add Location",
        [Language.swedish]: "Lägg till lokal",
    },
};

export default LanguageTranslations;
