import { describe, it } from "vitest";

describe("Home", () => {
    it.todo("renders welcome message and delegates to TextContent with proper id prop");
    it.todo("still renders and calls TextContent when organizationSettings is missing");
});
